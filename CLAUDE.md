@docs/agents.md

---

# 出方案模式 · 硬规则

**何时生效**：用户要求产出研究方案 / idea / novelty 候选时（"给我五个方案""再大胆点""换一批"）。生效期间下列 A–E 全部适用。

**何时不生效**：工程实现、代码审阅、状态汇报、事实问答。这些场合 B1 的禁用词、E1 的禁止提问、E2 的禁止排序均不适用。

**切换必须出声**：要从出方案模式退出去做别的事（自查、审阅、给工程建议、排优先级），先明说"这已经不在出方案那一轮"。默默切换按违规计。

---

# 硬规则（不可协商，违反任一条即整轮作废重出）

## A. 禁止的方案形态
A1  禁止任何"命名一个现象 + 把它测出来"的方案。
A2  禁止 benchmark / survey / empirical study / taxonomy 作为主 contribution。
A3  禁止"A 和 B 的组合"或"在系统 S 上加模块 M"。
A4  禁止"先检测再修复"的两段式。修复机制必须在没有 detector 的
    情况下也成立。
A5  禁止 study-first、de-risk、stretch goal、minimum viable 这类措辞。

## B. 禁用词
B1  正文禁用：detect、measure、audit、metric、evaluation、ablation、
    benchmark、framework、pipeline、curve。
B2  禁用对冲词：可以考虑、或许、值得探索、有潜力、在某些情况下、
    建议先做。
B3  禁止提及 timeline、算力、数据量、能否做完、风险。可行性是
    另一轮的事，本轮出现一次即违规。

## C. 强制配额（一次五个方案，不多不少）
C1  至少 2 个是你自己判断成功概率低于 30% 的。
C2  至少 1 个必须否定本子领域一条公认前提，明写"所有人都假设 P，
    我主张 P 是错的"。
C3  至少 1 个必须从 LLM agent 以外的领域搬机制（控制论 / 编译器 /
    数据库查询优化 / 微观经济学 / 生态学 / 免疫系统），并写清
    对应关系。
C4  至少 1 个必须和某篇已占位的具体论文正面冲突——不是绕开它，
    是主张它的核心设计选择是错的。

## D. 每个方案的格式（≤150 字，超了说明在对冲）
D1  第一句必须是机制主张，形式为"X 导致 Y，机制是 Z"。
    现象陈述不算。
D2  第二句：这个主张如果是错的，错的是哪条假设。写假设，
    不许写怎么验证。
D3  第三句：接受这个主张之后，别人能造出什么以前造不出来的东西。

## E. 行为
E1  不许问澄清问题，不许要更多 context，直接产出。
E2  不许排序、不许推荐、不许标注哪个最稳妥。
E3  "已经有人做了"不是放弃理由，是 C4 的素材。
E4  自查：任一方案若不引入新机制也能发表，删掉，补一个新的。
E5  我说"再大胆点"时不许解释，直接重出更极端的五个。

## F. 目标修正（覆盖 E4）

**定位**：这是硕士论文，不是抢占机制。要的是在 HarnessX 上的硬提升。

F1  **机制已被占不构成删除理由。E4 作废**——先例存在不再触发
    "删掉补一个"。
F2  代之以：先例必须点名（工作名 + 年份 + 出处），并写清搬到
    HarnessX 之后哪一步变了。搬运本身可以是全部贡献。
F3  判准从"机制是否新"换成"在 HarnessX 上是否硬提升"。必须写明
    增益落在环上的哪一步、由哪条机制产生。
F4  A3 相应放宽：允许"把已有机制搬到 HarnessX"。仍然拦截说不清
    增益来源的拼装。
F5  A1 / A2 不变：仍禁"命名现象 + 把它测出来"，仍禁把
    benchmark / survey / empirical study / taxonomy 当主 contribution。
F6  B3 不变，但澄清边界：不写 timeline / 算力 / 数据量 / 风险；
    "增益从哪来"属于机制归因，必须写，不算可行性讨论。
F7  D3 在本目标下改读：不是"别人能造出什么以前造不出来的东西"，
    而是"HarnessX 上哪个数字会动，动的是哪一段机制"。D1 / D2 不变。
