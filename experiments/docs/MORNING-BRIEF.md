# 早间简报(Aug-03 夜间自主工作)

> 一页看完。细节在 `RUN-LOG.md`,偏差在 `PAPER-METHODOLOGY-DEVIATIONS.md`。
> **需要你拍板的在最后一节。**

---

## 1. 实验状态

| 跑 | 状态 |
|---|---|
| **e_pervar3** | 🟢 02:41 起跑。**06:56 被外部杀死,07:03 已恢复续跑**(详见下方 M-40)。参数与 s1k8b103 全字段 diff **残余差异为空**,唯一变量 `regression_baseline=per_variant` |
| ~~e_pervar~~ | ❌ 作废:漏传 `--candidates-per-round 4` |
| ~~e_pervar2~~ | ❌ 作废:M-38 修复前发的车 |

**两次作废都是我的错,原因和处置都记在 RUN-LOG 与偏差登记里。**

---

## 1.5 🔴 夜里出过一次意外(已处理,但你该知道)

**06:56:55 运行被外部杀死** —— 日志中途戛然而止,**无异常无 traceback**,存活 4h15m。
不是崩溃。原因不明,日志无线索。**若再次在相近时长后死亡,则有系统性原因需另查。**

R0/R1/R2 已结算且完好,`--resume` 从 R3 续上,**没有任何测量被覆盖或丢失**。

**但 resume 暴露了一个通用缺口(M-40)**,差点让恢复本身失败:

V1 是 R2 结算末尾 fork 出来的(持 **14 题**),它的配置快照要到**下一轮首次测量**才写;
而 resume 只扫描「≤ 末结算轮」,于是找不到,**返回一个已知不存在的路径并只警告不阻断**。
R3 必然要测 V1 ⇒ 必然崩。

**处置**:把 V1 的来源候选 `C-R2-01` 的**原始**配置补写为 `R2/active_pool/V1/config.yaml`。
fork 时 `child.config_path = candidate.config_path`,**候选配置就是 V1 的配置**;
该候选两次尝试逐位相同(sha `a914cec4…`),无歧义。**补的是指针,不是测量值**,
同目录留了 `RESTORED.md` 说明来历。

**没有改代码** —— 纪律是运行期间冻结 runner 代码,否则 auto-resume 会造成同跑版本漂移。
代码修复留到本跑结束:resume 应从 `selected_candidate_ids` 反解来源候选,
而不是返回一个它自己已经判定为缺失的路径。**"警告后带着坏指针继续"是最差的一种行为** ——
既不 fail-closed 也不自愈。

---

## 2. 夜里修了什么

### 🔴 先读这条:我停跑重发的理由**不成立**

我把 M-37 升级成"值得停跑",论证是:工具装不上 ⇒ 演化器拿不到 Level-2 往返证据
⇒ 工具杠杆结构性失效。

**那个论证是错的,而且 M-22 早就解决了这个问题。**

`--l2-cert auto`(默认档,本跑在用)**从候选的【闸门评测轨迹】取证**,
不从演化器自测取证;而闸门走 `_prepare_round_config`,路径在那里是修好的。
**工具在闸门路径上装得上、证据拿得到。** 演化器亲笔申报优先但缺失不致命。

实跑佐证 —— Critic 对 C-R2-03 的裁定逐字写着:
*"in repo manifest mode the deterministic gate certifies this from real
trajectories (deviation M-22); recorded as strategy concern rather than rejection"*。

**结论**:M-38 的修复正确、四层验证通过、覆盖面约 88% 候选自测 —— 这些不变。
**错的是升级它的论证。** 它的真实后果就是 M-37 的原始表述(自我评估失真),
而我原本判 M-37"不值得停跑"—— **那个判断才是对的**。3 小时赔在不成立的前提上。

⚠️ 引用 M-38 时**不得再用"工具杠杆结构性失效"**。

### M-38 · 演化器不知道 `file:` 路径该怎么写(修复本身仍然正确)

它写 `file:///D:\…` 三斜杠,加载器固定砍 7 字符 ⇒ 余下 `/D:\…` ⇒ 解析成 `D:\D:\…` ⇒ 打不开。

追下去发现两件事:**①它看不见**(异常被吞进日志,观察流里没有信号,不可能自我纠正);
**②从没人告诉过它格式**(提示词零字提及,我们一直在下游默默改写)。

~~最重的一条:工具杠杆近乎结构性失效~~ → **该论证已作废,见上方红框**。
实际后果是**演化器自我评估失真**:它在缺少自己所写产物的条件下判断自己的候选。

**验证四层**:①单元 13 项(核心是**验 brief 说的是真话** —— 教错格式比不教更糟);
②孤立探针同模型各 10 次:**无 brief 0/10 → 有 brief 10/10**;
③**实跑验证(04:4x,e_pervar3 R2)** —— 首个写出产物的候选 `C-R2-04` 写了个 processor:

```
_target_: file://D:\…\processors\search_budget_guard.py::SearchBudgetGuardProcessor
                ^^ 两斜杠 | 三斜杠 0 次 | 运行期解析成功、文件存在
```

④**工具路径终验(04:47)** —— `C-R2-03/retry_01` 写了工具,
与作废跑失败的**同一目录形状**逐位对照:

```
e_pervar2  file:///D:\…\C-R1-02\retry_01\tools\pdf_text_tool.py     三斜杠,加载失败
e_pervar3  file://D:\…\C-R2-03\retry_01\tools\fetch_and_extract.py  两斜杠,已 import
```

**`tools/__pycache__` 存在** —— Python 只在模块**真被 import** 时生成。
这是"工具确实被自测加载"的**物证**,不是"路径看起来对"。

⇒ **M-37 的失效通道已闭合**:演化器现在能在自测中真正装载自己写的产物。

⚠️ 但 C-R2-03 的 config **随后被改写**,最终版只声明 processor 不声明工具。04:47 那次核验在当时为真,**不可据此说"工具候选通过了闸门"**。

⚠️ **影响面我此前说窄了。** 原表述是"工具杠杆结构性失效",实测历史:

| 跑 | 候选 config | 含 `file:` 目标 | 种类 |
|---|---|---|---|
| s1k8b103 | 34 | **30(88%)** | processors 36 / templates 8 / **tools 仅 4** |
| s2k8b50 | 31 | 15(48%) | processors 13 / tools 3 |

**正确表述:M-38 之前约 88% 的候选,其产物目标在自身自测中静默加载失败。**
工具只是最小的那个子集。

### M-39 · CH4 §4.6.1 的声明当时是假的

章节写「预算由构造配平」,代码里每子任务各拿全额(实测 2.2×)。
已实现 `--decomp-budget shared`,并发现**固定上限替代不了** ——
103 题计划子任务数 1–12,固定 4 仍 25% 超预算,**除法必须逐题**。

### 两处工具错误(都改变了已在引用的数字)

**去偏公式与正文不符**。初版给 1.33 SD,正文写 1.8 SD。改用 Blom 近似,n=16 得 **1.77 SD ≈ 8.1pp**。

**噪声地板未随床缩放**。写死 4.57pp(n=103),30 题床应是 8.47pp。

⚠️ **`a1big4` 的 −13.3pp 漂移此前被当作噪声地板旁证 —— 去偏后只剩 −4.4pp**,
远在该床 2SD 门槛(16.9pp)内。**以后一律引用去偏值。**

⚠️ 去偏值是**真实漂移的上界不是点估计**(峰值含真实改进时会减多)。
s2k8b50 把这点暴露得很清楚:12 候选零改善、原始漂移 0.0,去偏读作 +11.0 —— 那是过度修正在显形。

---

## 2.5 夜里挖出的三项发现(都影响论文内容)

### ① 勘误:「系统拿不到失败归因」是错的 —— 而真相更有价值

我此前说 `implicated_components` 全跑 1,648 条为空、`failure_category` 只是难度改名。
**测量没错,但下错了对象。** 是两套记录:

| 记录 | `failure_category` | `implicated_components` |
|---|---|---|
| `task_history.jsonl`(**持久化**) | `gaia_level_1/2/3` 确是难度改名 | **1,648 条全空** |
| `pipeline_audit.json`(**LLM Digester 本轮产出**) | `budget_exceeded` / `tool_output_dropped` / `scope_ambiguity` **真类别** | **非空**:`tools/Bash`、`processor/TokenBudgetProcessor`、`tools/WebFetch`、`model_capability` |

实测占比:e_pervar3 R1 **30/103**;s1k8b103 逐轮 **20–80%**。

**真正的缺陷**:归因**每轮算一次、每轮丢一次**。交给下一轮的 `prior_history` 里
`failure_category: null`,`implicated_components` 字段根本不存在。

⇒ 演化器看得到「这题**这一轮**栽在 Bash 上」,**永远看不到「它连续五轮都栽在 Bash 上」**。

**这比原说法强**:不是"缺少信号",而是"信号被算出来又扔掉",**修复成本极低(持久化即可)**。
可作为 CH3 的机制发现,也可能是一条低成本的改进臂。

### ② 能力划分只有 43% 可重现

两次独立分解同一批 103 题:

```
逐题标签完全相同   44/103 = 42.7%
类型集合 Jaccard    0.781
Rand 指数           0.779

但边际频率很稳:search 96→95 / browse 64→57 / compute 81→76 / verify 63→65
```

**"某类能力有没有"是稳的,"某题的确切类型集合"不稳。** 那个划分有一多半是一次采样的产物。

**已修完**。三遍分解取**逐类型多数票**(边际稳 ⇒ 按类型投票远比按集合稳)。
不稳是**系统性的**:三遍两两一致度 42.7% / 44.1% / 46.1%,不是一次意外。

留一验证(三种两遍组合各建共识再互比,**零额外 API**):

| | 逐题相同 | Jaccard |
|---|---|---|
| 原始遍两两 | 42.7–46.1% | 0.77–0.78 |
| **共识两两** | **63.1–66.0%** | **0.84–0.87** |

**改善实质,但仍只有约 64%** —— 照实写进威胁章,不四舍五入成"稳定"。

**划分也变好了**:

```
gaia_level(现状)   3 簇  → 有效池上限 3
单遍 + 合并          5 簇  → 上限 5
三遍共识 + 合并      7 簇  [34,17,16,10,9,9,8] → 上限 7   ← 采用
```

冻结包已改指 `task_clusters_consensus.json`。

⚠️ 这条无论如何都要进威胁章:**LLM 分解器是个有噪声的仪器**,
任何拿它的输出做路由的工作都欠读者这个数字。

### ③ e_pervar3 的 R1 零候选

```
actionability 0.85(阈值 0.5) | short_circuit: planner_empty_landscape | 候选 0
```

Planner 自述「**没有前轮可比**」—— **和发现 ① 直接咬合**。

已排除是我改动造成的(M-38 的 brief 注入的是 Evolver,短路在上游 Planner)。
一个待验机制:本跑 R0 = **69.9%**,比 s1k8b103 的 64.1% 高 5.8pp ⇒ 失败样本更少 ⇒ landscape 更薄。

~~M-38 至今未被真正检验~~ → **R2 已验证通过**,见 §2 M-38 第三层验证。

---

## 3. 新建的规格与工具

| 文件 | 作用 |
|---|---|
| `PREFLIGHT.md` | 发车前检查清单,**每条对应一次真实事故** |
| `FREEZE-CAPABILITY-ARM.md` | 改造臂冻结包 |
| `FREEZE-CH4-ARMS.md` | CH4 四臂冻结包,主读数 `B2 − B1` |
| `ARM-LADDER-DECISIONS.md` | **待你拍板的三条可选臂** |
| `build_consensus_clusters.py` | 多次分解取逐类型多数票,修划分不稳(见 §2.5 ②) |
| `compare_arms.py` | 按冻结包规则读出四类读数,**写在结果之前** |
| `export_frozen_plans.py` | 导出四臂共用的重放计划 |

四条 CH4 命令已干跑解析,并反向验过"B2 配并发会被拒"。

---

## 4. 需要你拍板

### 🔴 缺口一:没有 K=1 对照,复现不了论文 headline

论文 Table 5 的核心主张是 **Ensemble 阻止了 Global 的退化**(0.0 vs −24.3)。
我们没有 K=1 臂。早先我判过"不该跑",理由是它要对照的 s1k8b103 已撤回 ——
**该理由现在失效了**,e_pervar3 是干净的。

**成本 22h。我认为这条最值得加** —— 它把 CH3 从"我们改进了复现"
提升到"我们复现并检验了论文的核心机制主张"。而且
「论文的 Final−Peak 在构造上向下偏」这一条,**只有跑了 K=1 才有实测支撑**。

### 🔴 缺口二:归因被算出来又扔掉(**夜里新发现,我觉得这条最有意思**)

见 §2.5 ①。LLM Digester 每轮产出真实组件级归因,但 `prior_history` 不带它,
所以演化器**永远看不到"这题连续五轮都栽在同一个组件上"**。

论文 §4.3 p.10 自己说这段历史的用途是让 Planner
*"distinguish persistent failures from transient noise"* —— **没有归因就做不到。**
而 e_pervar3 的 R1 恰好短路于 `planner_empty_landscape`,理由是「没有前轮可比」。

**可选臂 C(`E-attrib`)**:把归因写进持久化 digest 并让历史带上它。
改动小、旗标化、**22h**。

**这是唯一一条由我们自己的诊断推出、且论文原文明确要求其功能的修复。**
比"换个分簇维度"更贴近论文自身的机制主张。

**我的优先级:K=1 > 臂 C > 缺口三。**

### 🟡 缺口三:改造臂一次改两个东西

`--cluster-source` 与 `--epsilon` 同时改,准确率上分不出各自份额。
可加 `E-capability-noeps`(能力分簇,ε=0),**22h**。
它是三条 CH3 臂里**唯一有事前预测**的(重放预测它更差),故可被证伪。

### 🟢 其余

ε 取 **0.05**(冻结包已定,保守档);要改说一声。

---

## 5. 排期

```
e_pervar3            进行中,约 28h
E-capability          22h  ← 已定
[K=1 对照]            22h  ← 待拍板(优先级 1)
[E-attrib 归因持久化]  22h  ← 待拍板(优先级 2)
[E-capability-noeps]  22h  ← 待拍板(优先级 3)
CH4 四臂               8h
──────────────────────────
全上约 124h ≈ 5.2 天,距 9/5 有 33 天
```

**机时不是瓶颈。**
