# 风险与错误登记册(2026-08-01 建,用户令「把所有错误以及可能导致问题的点列出来」)

> 本文件汇总:①我方已发生的判断错误 ②工具/代码缺陷 ③论文未规定而我方自填的空白
> ④已申报的主动偏差 ⑤数据层效度威胁 ⑥论文文档缺陷 ⑦流程失效。
>
> **严重度**:🔴 会让结论站不住 / 交稿阻塞 · 🟠 必须在论文里申报,否则算隐瞒 ·
> 🟡 该修不致命 · ⚪ 已修,留档防复发
>
> **口径纪律**:本表每条都注明**证据等级**——「亲验」=主循环直查代码/PDF/数据;
> 「工具」=分析工具输出;「二手」=子 agent 报告未经复核。**二手条目不得进论文证据链。**

---

## A. 已发生的判断错误(我方)

| # | 错误 | 状态 | 教训 |
|---|---|---|---|
| A1 | ⚪ 判"论文 p.11 收窄 p.8 ⇒ 我们回退基线读错了" | **已作废**(双盲推翻,亲验) | p.11 冒号后的定义是**测试集**范围,不是基线范围;我们的 T_k 限制正确 |
| A2 | ⚪ 称"候选不许弄错全部 96 道题" | **已更正** | `gate.py:224` 循环只遍历 `tk_results`;实为「自己簇内被解出过的题」 |
| A3 | ⚪ 给 coder 的任务书写"测试基线 874" | **已更正为 853** | 数字源自我方 SPEC §7.20,而该节文档落在主分支、代码在 worktree(见 B5) |
| A4 | ⚪ 让 agent 把 R14 成绩写进 ch3,当时 R15 在飞 | **已改**(现为终值) | 违反「完跑前不得作结果引用」;我下的指令本身违纪 |
| A5 | ⚪ VBS 用 `printf` 写路径,`\r` 被当回车 | 已修 | 路径含 `\r`/`\n`/`\t` 时必须用字面量参数形式 |
| A6 | ⚪ 研究任务书里写自己的倾向,锚定了 subagent | 已立规 | 两次论文过度解读的共同根因;**任务书禁述立场** |

**A 的元教训**:上述 A1/A2 都是「只读一句话就下结论」。**凡承重判断必须双盲或全文亲验。**

---

## B. 工具 / 代码缺陷

| # | 缺陷 | 严重度 | 状态 |
|---|---|---|---|
| B1 | `acceptance_report.py` 默认只找 `<tag>.console.log`,漏 `.resume.console.log` ⇒ **已完跑退出码 0 的实验被报成 IN-FLIGHT**,墙钟 n/a、traceback 计数为 0 | 🔴 | **待修**。论文审计附录会引到错误运行状态 |
| B2 | 同上工具 `--console-log` 只接受单路径,而本跑横跨两份日志 ⇒ **没有任何一次调用能给出全跑总成本** | 🟠 | 待修(改为可多传 / 自动发现同 tag 全部日志) |
| B3 | **`regression_baseline` 不进 lock**(仅非默认值写 provenance_warnings)⇒ 从 lock 看不出本跑用的哪个基线 | 🟠 | 待修。M-23 标为高危却零留痕,审计链断点 |
| B4 | `deep_metrics.py` CAPTION_D 错引 "M-24 patience adjudication"(M-24 实为轮预算 off-by-one;patience 在台账中无 M 号) | ⚪ | 已修 (`3f7f4da`) |
| B5 | **SPEC §7.20 在 `exp/variant-pool` 上描述 `--reasoning-effort`,该分支代码零命中**(实现在未合并的 efc2a89);主分支宣称了自己没有的能力 | 🟠 | 已加警示框,**合并后须删** |
| B6 | `TypeCreditLedger` 只有 `matrix()` 出口无入口 ⇒ 裁 F 要求的 B1→B2 账本交接通道不存在 | ⚪ | 已建 `--decomp-ledger-from`(a2700b1,**未合并**) |
| B7 | matplotlib 缺失,曲线图走优雅降级 | 🟡 | 完跑后可装(勿动活跑 venv 的纪律已解除) |

---

## C. 论文未规定、我方自填(复现空白)

> 依据:双盲 B 路全文核验(43 页 + 附录 + 数值扫描)。**这些不是"我们做错了",
> 而是"论文没写、任何复现者都必须自己定"**——但**每一条都必须在论文里显式声明**,
> 否则读者会误以为是照抄。

| # | 空白 | 我方选择 | 论文原文状况 | 严重度 |
|---|---|---|---|---|
| **C1** | **路由簇是什么** | GAIA 三档难度(L1/L2/L3) | 🔴 **全篇从未定义**。只在附录列过 GAIA **失败类型簇**(blocked-source 39% / reasoning 33% / figure-visual 11% / doc-table 11% / scope 6%),且**明说那不是路由簇** | 🔴 |
| **C2** | **回退基线** | `global`(全历史跨变体) | 正文 p.8「recorded in Tt」(全历史);**附录 p.30/p.43 却写 `regressions.md # tasks worsened vs R<n-1>`(跟上一轮比)**。**第三种读法我方两个开关都不实现** | 🟠 |
| **C3** | **单发 / 多发 ship** | 单发(Algorithm 1 的 `break`) | Algorithm 1 单发;**附录 p.34 明写多发**(bucket-disjoint 可同轮齐发)。若论文实跑是多发,我方 ship 率结构性偏低 | 🟠 |
| C4 | 池上限 K | 8 | **全篇无数值**(数值扫描确认) | 🟠 |
| C5 | 可行动性阈 α | 0.5 | 无数值;`at` 的定义与量纲亦未给 | 🟡(lock 已申报 "OURS") |
| C6 | target 变体选谁 | worst_first | 完全未定义 | 🟡(lock 已申报 "OURS") |
| C7 | fork 时新变体如何播种、领哪些题 | transfer(改进题转移给子代) | 未定义 | 🟡 |
| C8 | 退休用什么指标 | task_macro | 只说 "lowest-performing",指标未定义 | 🟡 |
| C9 | ±5% 噪声阈是否进门 | 不进(门逐题零容忍) | 未说明 | 🟡(=M-25,已申报) |
| C10 | R0 是否产出编辑 | 不进引擎,纯基线 | 未明确 | 🟡(=M-24,已申报) |
| C11 | 门的「改进检查」 | `if not improved: REJECT` | 枚举的四步门里**没有**改进检查,但 §4.5 又要求判定改进 | 🟡 |
| C12 | "counterfactual gate" | 未实现 | 全篇只出现一次,从未定义 | ⚪ |

---

## D. 已申报的主动偏差(台账在册)

| # | 偏差 | 编号 | 风险 |
|---|---|---|---|
| D1 | **种子 1 vs 论文 3/cell** | 已声明 | 🟠 **方差不可估**;后续只能借 S1 的 n=8 噪声估计代用,须写明是代用 |
| D2 | patience 16 vs 论文 3 | M-24 同类 | 🟡 一跑两得,可后验重建 p3 停点(已验证成立) |
| D3 | R0 不进引擎 ⇒ 15 轮实得 14 适应轮 | M-24 | 🟡 已声明 |
| D4 | Serper 替换 WebSearch | W1 | 🟡 lock 已记 |
| D5 | `evolve_commit_bounce=on` | F-A | 🟠 增加 meta 调用,与 off 不可字节比对 |
| D6 | `regression_accountability=shipped_only` | F-B | 🟠 **改变了哪些轮 no-op**,与 strict 不可字节比对 |
| D7 | lock 自记 `candidate_pipeline_semantics = runnable_fallback_not_full_llm_aegis_reproduction` | — | 🟠 **我方自己声明这不是完整 LLM AEGIS 复现**,论文里必须照实写 |
| D8 | 模型不同:我方 DeepSeek-V4 pro/flash;论文 meta=Opus 4.6,task=Sonnet 4.6 / GPT-5.4 / Qwen3.5-9B | — | 🟠 跨纪元不可直接比数,只能比**形态** |

---

## E. 数据层效度威胁(s1k8b103)

| # | 威胁 | 数字 | 影响 |
|---|---|---|---|
| **E1** | **预算耗尽率 19.4%** | 994 次 rollout 撞 per-candidate 帽,末轮 40 | 🔴 **全局天花板效应**——所有臂同等受压,这里每个分数都不是能力上限。**必须进威胁章** |
| **E2** | **十六轮零 APPLY** | 7 次全 FORK,2 次 REJECT(均因改进=0) | 🟠 机制从未整合,只分裂 |
| **E3** | **8 个变体 5 个是僵尸** | 末轮仅 V0/V6/V7 有领地 | 🟠 演化力气分散,七次改动只一条血脉产出 |
| **E4** | **真实演化增益比总分小** | +10.7pp 里,L1 的 +2 题由**从未被修改过的 V0** 贡献 = 噪声;真增益 ≈ +9 题 ≈ 8.7pp,**压在 ±9.1pp 噪声带边缘** | 🔴 论文里必须拆开报,不能只报 +10.7 |
| E5 | L3 只有 12 题 | 25.0% → 50.0% = 3 题 | 🟠 只描述、不做检验(已在 metric G caption 写死) |
| E6 | best-of-pool 93.2% 是 **pass@32 量级** | 96/103 | 🟠 **禁止**与单轮 pass@2 相减报"路由损失"(工具已内建拒绝+口径标注) |
| E7 | 事后最优路由 87.3% 带双重偏差 | 事后选择 + 观测偏差(多数 变体×任务 格从未观测) | 🟠 只作参照,不作因果主张 |
| E8 | **R1 峰值 80.6% 是 16 次带噪测量的最大值** | winner's curse | 🟠 冻结重测大概率低于 80.6;但 R1→R2 的 −16.5pp 是 3.6×SD,**损失是真的** |
| E9 | V0 与 V7 从未同轮跑同批题 | — | 🟠 "V7 比 V0 强"是**跨时点同题集**推论,非同场对决 |
| E10 | Serper 实耗日志不可测 | 成功调用不记日志 | 🟡 只能读 dashboard;失败计数 37 是枯竭签名 |
| E11 | R15 期间端点 429 限流 | 吞吐掉约 4 倍 | ⚪ 数据未损(4,015 条结果行齐全),仅影响排期 |
| E12 | 66 traceback / 29 ERROR | 42× `I/O operation on closed pipe` 等收尾噪声 | ⚪ 已逐条分类,判定跑是干净的 |

---

## F. 论文文档层面

| # | 问题 | 严重度 |
|---|---|---|
| F1 | **main.tex 三处 `TODO(user)`**:`ucl_logo.png` 全库缺失(logo 行现被注释)/ 提交日期 / **免责声明 Either-Or 必须二选一删一段** | 🔴 交稿阻塞,**只有用户能做** |
| F2 | 摘要仍是 `[PENDING]` | 🔴 交稿阻塞(计划最后写) |
| F3 | **ch5 / ch6 / ch8 / appB 仍是空壳** | 🔴 |
| F4 | 6 条工作名与正式标题不符(TRACE / OneFlow / HERA / Topaz / E3 / Leni),正文散文用名与题录不一致 | 🟠 |
| F5 | **md ↔ tex 已分叉**:ch7 markdown 有 5 条冻结中文引句,tex 只转过 4 条(§7.4.5 那条 in-sample 注从未进 tex) | 🟠 |
| F6 | README-PAPER.md 计数过期(仍写 5 个 CJK 占位 / 5 个 ADJ 占位,实际均为 0) | 🟡 |
| F7 | 5 处 `TODO(strip-before-submission)` 未清(**有意保留至交稿前**) | 🟡 |
| F8 | **体量警报**:导师要 ~50 页,现五章+AppA 已 ~36 页,补齐后恐 50–60 ⇒ 修订期要**瘦身不是加量** | 🟠 |
| F9 | **题目已改两次,未知会导师**(草稿在 `SUPERVISOR-EMAIL-DRAFT-JUL31.md`) | 🟠 用户动作 |
| F10 | **并发 3→10**;S1-FREEZE 写死「报备口径即运行口径,提速前须重新报备」,而记录只写「导师口径由用户更新」,**是否已告知只有用户知道** | 🟠 用户动作 |
| F11 | `distrbuted` 拼写已修 ⇒ **偏离了 UCL 模板字面**,用户可选择回退 | ⚪ |

---

## G. 流程失效(已发生,已立规)

| # | 失效 | 后果 | 现行规则 |
|---|---|---|---|
| G1 | 研究任务书陈述我方倾向 | 两次 subagent 过度解读论文 | **任务书禁述立场** |
| G2 | 摘要器/元数据层的结论被当承重证据 | HarnessX manifest 判词二次反转;"AEGIS 是内部代称"误报 | **证伪也必须直查全文** |
| G3 | SPEC 条目与代码落在不同分支 | 错数 874 经任务书传给下游 | **worktree 建功能时,SPEC 与代码同分支落地** |
| G4 | RUN-LOG 台账停在「R1 pending」 | 三路 review 各自撞同一洞;全部动力学叙述一度只靠 curve_extract 支撑 | 已补记,批次收尾须核四表面 |
| G5 | MAP 类文件把 S5 结论与二手印象混排 | 跨 session 传播后假扮事实 | **判词须标证据等级**(本表已执行) |

---

## 优先级(我的建议)

**必须在下一步动作前处理**
1. 🔴 **B1 修 acceptance_report 日志选择** —— 否则审计附录引到错状态
2. 🔴 **E1 + E4 写进威胁章** —— 预算耗尽 19.4%、真增益拆解
3. 🔴 **F1 三处 user TODO** —— 只有用户能做

**决定论文形状,须尽早裁**
4. 🔴 **C1 路由簇** —— 论文空白 + 直接决定漂移形态,是**给导师那封信应问的问题**
5. 🟠 **C2 / C3** —— 两条论文自相矛盾处,都可能独立解释我方曲线

**清账**
6. 🟠 B2 / B3 / B5 三个审计缺口
7. 🟠 F4 / F5 / F8 文档一致性与体量

---

*本表随发现更新;新增条目须带证据等级与状态。*
